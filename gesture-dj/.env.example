# ===========================================
# Gesture DJ — Environment Variables
# ===========================================
# Copy this file to .env.local and fill in values:
#   cp .env.example .env.local

# --- Supabase (required for auth + sessions) ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# --- Flowglad (billing / vote credits) ---
FLOWGLAD_SECRET_KEY=sk_test_...
NEXT_PUBLIC_FLOWGLAD_KEY=pk_test_...

# --- AI / Agent ---
DEDALUS_API_KEY=your-dedalus-api-key
K2THINK_API_KEY=your-k2think-api-key

# --- ElevenLabs (music generation) ---
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# --- Supabase Service Role (for storage uploads — keep secret!) ---
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# --- Runtime URLs (defaults work for local dev) ---
NEXT_PUBLIC_WS_URL=ws://localhost:8080
NEXT_PUBLIC_DJ_BOOTH_URL=http://localhost:8000
WS_SERVER_HTTP_URL=http://localhost:8080

# ===========================================
# Google OAuth Setup (in Supabase + Google Cloud)
# ===========================================
# 1. Create OAuth 2.0 Client ID at https://console.cloud.google.com/apis/credentials
# 2. Set Authorized JavaScript origins:
#      http://localhost:3000
# 3. Set Authorized redirect URIs:
#      https://YOUR_PROJECT.supabase.co/auth/v1/callback
# 4. In Supabase Dashboard → Auth → Providers → Google:
#      Paste Client ID and Client Secret
# 5. In Supabase Dashboard → Auth → URL Configuration:
#      Site URL: http://localhost:3000
#      Redirect URLs: http://localhost:3000/**
